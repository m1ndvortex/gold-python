# Cron configuration for automated backups
# Runs backup every day at 2:00 AM
0 2 * * * /usr/local/bin/backup.sh >> /var/log/backup.log 2>&1

# Weekly full backup on Sundays at 1:00 AM
0 1 * * 0 /usr/local/bin/backup.sh --full >> /var/log/backup.log 2>&1